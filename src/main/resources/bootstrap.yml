spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/peinanwei/
          username: peinanwei
          password: wwc123456
          search-paths: config-server
eureka:
  instance:
    prefer-ip-address: true
    non-secure-port: ${server.port}
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/
    enabled: true
server:
  port: 8083